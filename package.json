{
  "name": "scripts",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun -e 'await require(`.`).test()'",
    "check": "bun -e 'await require(`.`).checkDependencies()'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
